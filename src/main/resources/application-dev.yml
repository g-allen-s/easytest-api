
server:
  port: 8082
  connection-timeout: 18000000
  servlet:
    session:
      timeout: 30m

spring:
  application:
    name: easytest
  datasource:
    url: jdbc:mysql://localhost:3306/easytest?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 858483
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.my.easytest.entity
  configuration:
    mapUnderscoreToCamelCase: true

logging:
  level:
    com.my.easytest.dao: debug #打印sql


map:
  local:
    env: 开发环境
